const superHeros: string[] = []

type User ={
    name: string
    isActive: boolean
}

const allUsers: User[] = []

const MLModels: number[][] = [
    [101,101,101,1],
    []
]

superHeros.push("spiderman")

allUsers.push({
    name: "",
    isActive: true
})